version: '3.8'

services:
  centra-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=development
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/"]
      interval: 30s
      timeout: 10s
      retries: 3
